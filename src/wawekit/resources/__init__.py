"""Bundled resources (icons, sample data).

Kept as a Python subpackage so assets can be loaded with importlib.resources,
which works inside a frozen PyInstaller build. Assets are added as modules
require them.
"""
